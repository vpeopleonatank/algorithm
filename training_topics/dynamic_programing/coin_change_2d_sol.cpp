#include <bits/stdc++.h>

#define fastio                      \
  ios_base::sync_with_stdio(false); \
  cin.tie(NULL)

#define endl "\n"

using namespace std;

int main() {
  fastio;
  int n, m;
  cin >> n >> m;
  long long c[m];
  for (int i = 0; i < m; ++i) {
    cin >> c[i];
  }
  long long f[m + 1][n + 1];

  return 0;
}
