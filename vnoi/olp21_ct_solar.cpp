#include <bits/stdc++.h>

#define fastio                      \
  ios_base::sync_with_stdio(false); \
  cin.tie(NULL)

#define endl "\n"

using namespace std;

// Not done
int main() {
  fastio;
  int n, q;
  cin >> n >> q;
  vector<int> a(n);
  for (int i = 0; i < n; ++i) {
    cin >> a[i];
  }
  vector<int> t(n);
  for (int i = 0; i < q; ++i) {
    int o;
    cin >> o;
    if (o == 1) {
      int d;
      cin >> d;

    }
  }
  return 0;
}
